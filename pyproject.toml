[project]
name = "symbolic-regression"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mark_S", email = "bkp.01mks@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
symbolic-regression = "symbolic_regression:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
